import 'package:flutter/material.dart';

class GetUsers extends StatelessWidget {
  const GetUsers({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text(" total user number... users list "),);
  }
}